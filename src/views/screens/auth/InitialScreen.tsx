{/* <NavigationContainer>
      {user ? (
        // caso tiver usuário, mostra tela dentro do app
        <RegisterScreen/>
      ) : (
        // caso não tiver usuário, mostra apenas login e registro
        <LoginScreen/>
      )}
    </NavigationContainer> */}